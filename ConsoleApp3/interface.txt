#mehtod overloading in c# is a feature that allows a class to have multiple methods with the same name but different parameters. This means that you can define multiple versions of a method, each with a different number or type of parameters,
and the appropriate version will be called based on the arguments passed to the method.
#structuring method overloading in C# involves defining multiple methods with the same name but different parameter lists.
Here’s an example to illustrate method overloading:

#the sealed class 
sealed class Calculator
{
	// Method to add two integers
	public int Add(int a, int b)
	{
		return a + b;
	}
	// Overloaded method to add three integers
	public int Add(int a, int b, int c)
	{
		return a + b + c;
	}
	// Overloaded method to add two doubles
	public double Add(double a, double b)
	{
		return a + b;
	}
}
#abstract class 
abstract class Shape
{
	// Abstract method to calculate area
	public abstract double CalculateArea();
	// Abstract method to calculate perimeter
	public abstract double CalculatePerimeter();
}
#abstract class Circle : Shape
{
	private double radius;
	public Circle(double radius)
	{
		this.radius = radius;
	}
	// Implementing the abstract method to calculate area
	public override double CalculateArea()
	{
		return Math.PI * radius * radius;
	}
	// Implementing the abstract method to calculate perimeter
	public override double CalculatePerimeter()
	{
		return 2 * Math.PI * radius;
	}
}
#interface
convvention for interface naming in C# is to prefix the interface name with an uppercase "I". 
This helps to distinguish interfaces from classes and other types in the code. For example, 
an interface for a shape could be named "IShape", while a class implementing that interface could be named "Circle" or "Rectangle".
interface IShape
{
	// Method to calculate area
	double CalculateArea();
	// Method to calculate perimeter
	double CalculatePerimeter();
}
interface ICircle : IShape
{
	// Method to get the radius of the circle
	double GetRadius();
}
purpose of using interfaces in C# is to define a contract that classes can implement.
#interface code example 
interface IShape
{
	double CalculateArea();
	double CalculatePerimeter();
}
interface Iperson
{
	string GetName();
	int GetAge();
}
#Deligate 
c# deligattesn are similar to the pointers to the functions in c and c++
deligate 

#events 
an event in C# is a way for a class to provide notifications to other classes or objects when something of interest occurs. 
Events are based on delegates, which are type-safe function pointers that can reference methods with a specific signature. 
When an event is raised, all the methods subscribed to that event are invoked.

#partial class
class partial class in C# allows you to split the definition of a class, struct, or interface across multiple files.
used by developers to organize code better, especially in large projects or when working with auto-generated code.
if class is marked as partial, it can be defined in multiple files, and the compiler will combine them into a single class at compile time.


syntax for defining a partial class: 
pariatl class ClassName
{
	// Class members go here
}


#collection teypes 
a collection type in C# is a data structure that can hold multiple elements, such as arrays, lists, dictionaries, and sets.
collection types provide a way to store, manage, and manipulate groups of related objects in a convenient and efficient manner.
collection types in C# can be broadly categorized into two main categories: generic and non-generic collections.
namespace System.Collections.Generic provides a set of generic collection types, 
such as List<T>, Dictionary<TKey, TValue>, and HashSet<T>, which allow you to work with strongly typed collections.


#collection types in C# can be broadly categorized into two main categories:\
generic and
non-generic collections.

#list 
syntax for defining a list in C#:
list<T> listName = new list<T>();
list <string> names = new list<string>();


#Array list 
Array list is a non-generic collection type in C# that can hold a variable number of elements of any type.
arraylist is part of the System.Collections namespace and provides a way to store and manipulate a 
collection of objects without specifying their types at compile time.

#exception handling 
An ecxeption in C# is an event that occurs during the execution of a program that disrupts the normal flow of instructions.
a c# exception is an object that represents an error or unexpected condition that occurs during the execution of a program.
exception handling in C# is the process of responding to and managing exceptions that may occur during the execution of a program.
exception handling in C# is typically done using try-catch blocks, which allow you to catch and handle exceptions in a structured way.
c# exception handling syntax:
try
{
	// Code that may throw an exception
}
catch (ExceptionType ex)
{
	// Code to handle the exception
}
finally
{
	// Code that will always execute, regardless of whether an exception occurred or not
}








